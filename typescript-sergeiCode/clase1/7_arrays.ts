let arregloNumeros: number[] = [1, 2, 3, 4, 5, 6]; // Infiere que es un arreglo de números, opero aún así debemos de declarar como arreglo de números
let arregloTexto: string[] = ['HTML', 'CSS', 'JS'] // Infiere que es un arreglo de Strigns

console.log(arregloTexto[0].indexOf('HTML'));